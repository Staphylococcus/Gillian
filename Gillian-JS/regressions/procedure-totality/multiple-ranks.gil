spec choose(n)
[[ (n == 0i) ]] [[ (ret == 0i) ]] variant(n) normal;
[[ (n == 1i) ]] [[ (ret == 1i) ]] normal
proc choose(n) {
  ret := n;
  return
};
spec main()
[[ emp ]] [[ (ret == 0i) ]] normal
proc main() {
  ret := "choose"(0i);
  return
};
