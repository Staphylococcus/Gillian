spec answer(flag)
[[ (flag == #flag) * types(#flag : Bool) ]] [[ (ret == 42i) ]] normal
proc answer(flag) { first := "helper"(false); ret := "helper"(true); return };
proc helper(flag) {
  goto [flag] spin done;
spin: goto spin;
done: ret := 42i; return
};
