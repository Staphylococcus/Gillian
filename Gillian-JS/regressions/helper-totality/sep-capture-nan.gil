spec answer(a)
[[ (a v== #a) * <Cell>(#a, "x"; nan) * <Cell>(#a, "kept"; 7i) ]]
[[ (ret v== nan) * <Cell>(#a, "x"; nan) * <Cell>(#a, "kept"; 7i) ]] normal
proc answer(a) {
  sep_assert (<Cell>(a, "x"; #captured)) [bind: #captured];
  assert (#captured v== nan);
  cell := [GetCell](a, "x");
  ret := l-nth(cell, 2i); return
};
