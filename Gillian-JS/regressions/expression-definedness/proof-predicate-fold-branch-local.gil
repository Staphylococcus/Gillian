pure nounfold pred Target(n : Bool) : (n == true);
spec answer(b)
[[ (b == #b) * types(#b : Bool) ]]
[[ (ret == 42i) ]] normal
proc answer(b) {
  goto [b] init done;
  init: x := 0i;
  done: fold Target(not (b and (x == 1i)));
  ret := 42i; return
};
