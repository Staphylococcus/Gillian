spec main()
[[ emp ]] [[ (ret == 42i) ]] normal
proc main() {
 fresh := [Alloc]($lclosed, null);
 ret := 42i;
 return
};

spec other()
[[ emp ]] [[ (ret == 42i) ]] normal
proc other() { ret := 42i; return };
